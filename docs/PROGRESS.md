# Progress

> Live state of the repository. Update after every meaningful work increment (sub-task done, blocker hit, decision made). Entries dated `YYYY-MM-DD`. Newest first.

## How to read this file

- Each dated section is a single working day (or session).
- Bullets are chronological inside a day.
- Each bullet states **what changed**, **why**, and **what's next** where relevant.
- After a session interruption, the last bullet of the latest day is the resume point.

## 2026-05-17

- Started `task/bootstrap-governance` from `main` (commit `c25dd4e` = initial). Goal: stand up the process governance — branch strategy, validation loop, Copilot review automation, docs scaffolding, repo tooling — **before** any product code is written.
- Wrote `AGENTS.md` as the single source of truth for any AI agent (Claude/Codex/Gemini/Copilot). Captured operating rules, stack defaults, security rules, branch and PR loop (with the GraphQL Copilot fallback from the reference repo `product_image_discovery_admin`), Definition of Done, sub-agent context handoff, self-resume protocol.
- Wrote `CLAUDE.md`, `GEMINI.md`, `.github/copilot-instructions.md` as thin delegators to `AGENTS.md` plus agent-specific notes (skill activation, permission profile, self-resume).
- Wrote `docs/RULES.md` with implementation defaults table, UI rules (density / no nested cards / radius ≤ 8px / dark mode mandatory), security rules (pre-write redaction, sandbox not optional, pack execution risk), testing rules (coverage ≥ 80% on new code), documentation rules, review rules, code style, commit style, git workflow.
- Completed Task 0 in one pass: repo scaffolding (`package.json` Bun workspaces, `tsconfig.base.json`, `biome.json`, `bunfig.toml`, `.editorconfig`, `.gitignore`, `.gitattributes`, `.nvmrc`, `.bun-version`).
- CI workflow `.github/workflows/ci.yml` with jobs: meta (markdown link check), typecheck+lint, unit tests (Bun + Node), build, e2e (Playwright). Placeholder gates pass while packages don't exist yet.
- Copilot review automation `.github/workflows/copilot-review.yml` with gh CLI fast-path + GraphQL fallback (per AGENTS.md).
- GitHub templates: CODEOWNERS, pull_request_template (DoD checklist), ISSUE_TEMPLATE bug/feature/security.
- Skills `aqa-process-loop` + `aqa-self-resume` mirrored across `.agents/`, `.claude/`, `.gemini/`, `.github/skills/`.
- Long-form docs: `docs/design/admin-panel-template.md` (full UI spec for parallel template work, 25 screens, tech stack, design tokens, wireframes), `docs/getting-started.md` (junior onboarding skeleton), `docs/ecosystem-explained.md` (concepts skeleton), `docs/adr/README.md` (ADR template + index of 10 critical ADRs), `docs/internal/implementation-plan.md` (full enterprise blueprint, maintainer-internal, Italian — see English audience note at top).
- `CONTRIBUTING.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, `CHANGELOG.md` (with 0.0.1-governance entry).
- `README.md` rewritten enterprise-wow (badges, TOC, features, junior-friendly quick start, multi-agent matrix, architecture, roadmap, status, docs links).
- Local gates green (`bun run lint`, `bun run typecheck`, `bun run test`, `bun run build` — placeholders while packages are empty).
- Pushed branch, opened PR #1, assigned Copilot reviewer (gh fast-path `--add-reviewer copilot-pull-request-reviewer` succeeded; token had `read:project` scope), CI ran.
- First CI run RED: Typecheck+Lint job failed with `Unknown lockfile version` because `.bun-version=1.1.40` pinned a Bun older than the one that produced `bun.lock`. Bumped `.bun-version` and `packageManager` to `1.3.11`. Captured in LESSON.
- Second CI run GREEN across all 6 jobs (Meta, Typecheck+Lint, Unit Bun, Unit Node 22 LTS, Build, E2E Playwright). Copilot and chatgpt-codex-connector both posted review with 15 inline comments combined.
- Addressed all 15 comments in one pass: removed maintainer-local paths from AGENTS.md/CLAUDE.md/RULES.md, moved implementation-plan into `docs/internal/` with English audience note, replaced echo-placeholder scripts with `scripts/run-workspace-script.mjs` (portable, propagates exit codes), made `prepare` cross-platform (`node -e ""`), removed test-file exclusions from `tsconfig.base.json`, deduped `.gitattributes`, hardened CI (advisory link-check uses `continue-on-error`, Node-test job propagates exit codes with `worst=$max`), hardened copilot-review workflow (verify step fails visibly when no reviewer assigned and no prior Copilot review exists), fixed Tailwind v4 dark-mode syntax in admin-panel-template (`@custom-variant dark` + `.dark { ... }` override), created stub files for the three docs README linked (`docs/architecture/reference.md`, `docs/security/threat-model.md`, `docs/methodology/agentic-qa.md`), removed unreleased `1.x` line from `SECURITY.md`. Biome override added for `scripts/**` to allow `console`.
- Local gates re-verified green (lint, typecheck, test, build).
- Next step: commit the fixes, push, re-request Copilot review (auto via workflow + manual rerequest if needed), wait for CI green + Copilot acknowledgment, merge PR #1 (squash + delete branch), tag `v0.0.1-governance`, then open Task 1 — schemas package.
- Iterated Copilot review loop 8 times on PR #1. Each pass surfaced legitimate but progressively narrower comments (P0 cross-platform issues → P1 silent failures → P2 cross-script consistency → P3 hardening edge cases). All 52 actionable comments addressed across the 8 iterations; CI consistently green; LESSON.md captures the patterns. Closing the loop now per the stopping criterion documented in LESSON.md ("after 7+ iterations, defer remaining P3 nits to a follow-up hardening issue rather than block the macro task"). Any further hardening of `scripts/run-*.mjs` belongs in a separate issue after Task 1 ships.
- **Task 0 closed.** PR #1 squash-merged into `main` as `d9cb4b3`. Branch `task/bootstrap-governance` deleted. Tag `v0.0.1-governance` pushed. Resume point: open `task/schemas-and-core-types` for Task 1 (packages/schemas: Zod + JSON schema for risk-map, scenario, finding, profile, config, run-manifest; dual-mode `execution_mode`; 3-level reproducibility; confidence_components; hash-chained audit log shape).
- **Task 1 — schemas implementation done.** `packages/schemas` created with Zod as source of truth + JSON Schema (Draft 2020-12) generated artifacts. Covered domains: Project, Profile, ProfilesFile, RiskMap, Scenario, Finding, Event, Run, PackManifest. Determinism contract from §3.1 of the plan codified in `Finding` (`reproducibility.{bug,scenario,agent}_level`, `verification_floor`, `confidence_components`) with a `superRefine` guard that rejects `status=verified` without a deterministic floor. Audit log codified in `Event` (`seq` + `prev_hash` + `hash` sha256). 9 valid fixtures + 8 invalid fixtures. 20 Bun tests green (validators + round-trip + determinism gating). Local gates all green (lint, typecheck, test, build). Next: open PR, request Copilot, iterate per process loop.
- **PR #2 Copilot review loop (4 passes, 29 actionable comments addressed).** Pass 1 (11 comments): fixed exclusiveMinimum Draft-4→2020-12, Slug case sensitivity, ReproLevel/Event/Finding cross-references, signing.sha256 reuse, must_hold tautology, clean script ESM-correctness, ProfilesFile fixture. Pass 2 (8 new): JSON Schema $schema declaration, ReproLevel attempts/successes invariant, Run terminal-state ↔ finished_at, LongSlug for run-ids, invalid-fixture error-path assertions, README install-instructions gating. Pass 3 (9 new): Run gating mirrored into JSON Schema if/then, zod peer-dep alignment, ProfilesFile key↔name superRefine, Slug regex tightening (no trailing/consecutive dashes), Finding.duplicate_of self-check + status='duplicate' requirement, tsconfig.incremental enablement. Pass 4 (3 new): common.ts comment accuracy, Run.totals .strict() Zod alignment with JSON-Schema additionalProperties:false, emit-script header documents the known limitation that some cross-field superRefines cannot be mirrored to pure JSON Schema. Stopping loop here per LESSON.md criterion — remaining flagged items are stale repeats Copilot keeps re-surfacing despite the fixes being committed.
- **Follow-up issue to file post-Task-1:** mirror remaining superRefines into JSON Schema where feasible (ReproLevel, ProfilesFile key↔name, Finding.duplicate_of). Also add an Ajv-based round-trip test that loads `schemas/v1/*.schema.json` and validates the same fixtures Zod uses, so the two layers cannot silently drift. Tracked in #3.
- **Task 1 closed.** PR #2 merged (`3871cd1`). Branch deleted.
- **Task 2 — CLI foundation done.** `packages/kit` created with `aqa` binary and three commands: `init` (scaffolds `.aqa/project.yaml`, `risk-map.yaml`, `profiles.yaml`, `testing.md`), `doctor` (runtime/agent-docs/validation checklist with pass/warn/fail rollup), `validate` (schema-checks `.aqa/*` via `@aqa/schemas` with precise error paths). Project profiler detects runtime/package-manager/framework/DB/LLM-SDK/test-runner/SUT type from package.json + filesystem signals (10 framework signals, 11 DB, 6 LLM, 5 test runner). 16 tests pass under both Bun and Node 22. Bin smoke-tested: `node dist/cli/aqa.js --version` returns `0.0.1`. All local gates green.
- **Task 2 closed.** PR #4 merged (`895cec9`). CI bun job + Node 22 job aligned to per-package script runner; CI now builds workspaces topologically (via run-workspace-script.mjs's new DFS sort) before typecheck/test so downstream packages can resolve workspace imports through dist/.
- **Task 3 — pack system + 5 base packs done.** `packages/pack-loader` (`@aqa/pack-loader`) discovers and validates packs via `@aqa/schemas`'s `PackManifest`, and exposes `appliesWhen()` which evaluates the 6-clause predicate (runtime / framework / db / sut_type / tags_any / tags_all) with AND semantics and "missing clause = any". 12 tests cover loader edge cases (no manifest, malformed manifest, multi-pack discovery) and all 7 applies_when clause combinations. Five base packs scaffolded under `packs/`: `core` (universal seed risks + templates), `api-core` (auth/idempotency/pagination/rate-limit risks with 2 scenarios), `web-ui` (form-loss/double-submit/auth-flow with 2 Playwright scenarios), `llm-agent` (prompt-injection/tool-misuse/handoff risks with 1 scenario), `security` (OWASP Top 10 + Agentic Top 10 as 12 seed risks). Template engine (eta/njk) and `aqa generate --packs ...` command deferred to a follow-up (see #5/#6 in the durable plan). All gates green; 49 tests pass repo-wide.
