# @aqa/pack-web-ui

Web UI baseline pack. Auto-applies when the SUT type is `web`.

Seed risks: form-loss-on-refresh, double-submit, post-login redirect safelist.
Seed scenarios use `playwright` probes (Task 5 wires the actual runner).
